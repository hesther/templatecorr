name: test
channels:

  - conda-forge

  - defaults dependencies:
    # Base depends
  - python
  - pip

    # Package dependencies
  - numpy
  - rdkit
  - pandas
    
  # Testing
  - pytest
  - pytest-cov
  - codecov

  # Pip-only installs
  #- pip:
  #  - codecov

