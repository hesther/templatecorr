name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - numpy
  - rdkit
  - pandas
  - joblib
  - tqdm

  # Pip-only installs
  - pip:
    - git+https://github.com/hesther/rdchiral.git
    - git+https://gitlab.com/mefortunato/template-relevance.git